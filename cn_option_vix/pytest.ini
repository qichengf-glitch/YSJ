[pytest]
# Run from the WORKSPACE ROOT with:  python -m pytest cn_option_vix/tests -v
# (python -m puts the workspace root on sys.path so `import cn_option_vix` and
#  `import container` both resolve.)
python_files = test_*.py
addopts = -q
